function moveToRegister() {
    const origin = window.location.origin;
    window.location.assign(`${origin}/register`);
}

function moveToLogin() {
    const origin = window.location.origin;
    window.location.assign(`${origin}/login`);
}